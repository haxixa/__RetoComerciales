<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.example.retocomerciales">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_envios"></activity>
        <activity android:name=".activity_calendario" />
        <activity android:name=".activity_pedido3" />
        <activity android:name=".activity_pedido2" />
        <activity android:name=".activity_pedido1" />
        <activity android:name=".activity_addPartner" />
        <activity android:name=".activity_gestionPartner" />
        <activity android:name=".activity_menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>